const Discord = module.require("discord.js");
module.exports.run = async (bot, message, args) => {
 //ru: Тут вы пишите код самой команды. En: Here you write the code of the command itself
};

module.exports.help = {
  name: "name" //ru: имя команды по которому она будет вызываться. En: the name of the command by which it will be called
};
